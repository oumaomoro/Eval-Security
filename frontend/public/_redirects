# 1. API Pass-through (High Priority)
/api/* https://api.costloci.com/api/:splat 200

# 2. Asset Integrity (Exclude from fallback)
/assets/* /assets/:splat 200
/favicon.png /favicon.png 200

# 3. SPA Fallback (Greedy Catch-all - lowest priority)
/* /index.html 200
/*      /index.html  200
